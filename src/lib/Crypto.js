//TODO: Token encryption / decryption
const encryptToken = (token) => token;
const decryptToken = (encryptedToken) => encryptedToken;

const Crypto = {
    encryptToken,
    decryptToken
}

export default Crypto